#!/usr/bin/env bash
# Deprecated: This script is no longer used.
# Use the Make targets from a WSL terminal instead:
#   make run      # run in foreground
#   make run-bg   # run in background (logs in /tmp/server.log)
#   make demo     # quick example requests
#   make stop     # stop background server
exit 0
